name: key-rotation
on:
  workflow_dispatch: {}
jobs:
  rotate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Rotate keys/certificates (placeholder)
        run: |
          echo "Rotate secrets in Key Vault and trigger pod restarts via label bump"
