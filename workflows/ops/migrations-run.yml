name: migrations-run
on:
  workflow_dispatch: {}
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run EF/Core migrations per service (placeholder)
        run: |
          echo "Run migrations for services: sales, inventory, users, catalog, payments"
