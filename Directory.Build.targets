<Project>
  <Target Name="EmitBuildInfo" AfterTargets="Build">
    <Message Importance="High" Text="Building $(MSBuildProjectName) ($(Configuration)) with .NET $(TargetFramework)" />
  </Target>
</Project>
