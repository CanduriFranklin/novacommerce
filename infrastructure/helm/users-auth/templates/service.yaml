apiVersion: v1
kind: Service
metadata:
  name: {{ include "users-auth.fullname" . }}
  labels:
    {{- include "users-auth.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "users-auth.selectorLabels" . | nindent 4 }}
