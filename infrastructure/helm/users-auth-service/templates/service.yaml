apiVersion: v1
kind: Service
metadata:
  name: users-auth-service
  labels:
    app.kubernetes.io/name: users-auth-service
    app.kubernetes.io/instance: default
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: users-auth-service
    app.kubernetes.io/instance: default
  ports:
    - name: http
      port: 80
      targetPort: 8080
