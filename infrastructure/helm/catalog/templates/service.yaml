apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  labels:
    app.kubernetes.io/name: catalog-service
    app.kubernetes.io/instance: default
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: catalog-service
    app.kubernetes.io/instance: default
  ports:
    - name: http
      port: 8080
      targetPort: 8080
