apiVersion: v1
kind: Service
metadata:
  name: sales-service
  labels:
    app.kubernetes.io/name: sales-service
    app.kubernetes.io/instance: default
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: sales-service
    app.kubernetes.io/instance: default
  ports:
    - name: http
      port: 80
      targetPort: 8080
