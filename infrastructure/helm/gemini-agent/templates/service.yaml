apiVersion: v1
kind: Service
metadata:
  name: {{ include "gemini-agent.fullname" . }}
  labels:
    {{- include "gemini-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gemini-agent.selectorLabels" . | nindent 4 }}
