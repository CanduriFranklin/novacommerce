apiVersion: v1
kind: Service
metadata:
  name: nl-sql-agent
  labels:
    app.kubernetes.io/name: nl-sql-agent
    app.kubernetes.io/instance: default
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: nl-sql-agent
    app.kubernetes.io/instance: default
  ports:
    - name: http
      port: 8080
      targetPort: 8080
